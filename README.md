# Learning Platform - Учебная платформа

Полнофункциональная учебная платформа на базе Spring Boot с использованием Hibernate/JPA и PostgreSQL.

## 📋 Описание

Веб-приложение для управления онлайн-курсами, которое включает в себя:

- Управление пользователями (студенты, преподаватели, администраторы)
- Создание и управление курсами с модульной структурой
- Систему записи студентов на курсы
- Домашние задания и их проверку
- Тестирование знаний (квизы)
- Систему отзывов и рейтингов курсов
- **REST API** для всех операций

## 🛠 Технологический стек

- **Java 17**
- **Spring Boot 3.5.7**
    - Spring Data JPA
    - Spring Web
    - Spring Validation
- **Hibernate** (JPA реализация)
- **PostgreSQL** (production database)
- **H2** (test database)
- **Lombok** (уменьшение boilerplate кода)
- **Docker Compose** (для PostgreSQL)
- **Gradle** (система сборки)
- **JUnit 5 + AssertJ** (тестирование)

## 📊 Модель данных

Приложение содержит **15 JPA сущностей** с различными типами связей:

### Основные сущности:

1. **User** - пользователи системы (студенты, преподаватели, администраторы)
2. **Profile** - профили пользователей с дополнительной информацией
3. **Category** - категории курсов (Программирование, Дизайн, и т.д.)
4. **Course** - учебные курсы
5. **Enrollment** - записи студентов на курсы
6. **Module** - модули курсов
7. **Lesson** - уроки внутри модулей
8. **Assignment** - домашние задания к урокам
9. **Submission** - отправленные решения заданий
10. **Quiz** - тесты для проверки знаний
11. **Question** - вопросы в тестах
12. **AnswerOption** - варианты ответов на вопросы
13. **QuizSubmission** - результаты прохождения тестов
14. **CourseReview** - отзывы студентов о курсах
15. **Tag** - теги для классификации курсов

### Типы связей:

- ✅ **One-to-One**: User ↔ Profile
- ✅ **One-to-Many**: Course → Module → Lesson → Assignment
- ✅ **Many-to-One**: обратные связи для всех One-to-Many
- ✅ **Many-to-Many**: Course ↔ Tag, User ↔ Course (через Enrollment)

## 🚀 Быстрый старт

### Предварительные требования

- Java 17 или выше
- Docker и Docker Compose (для PostgreSQL)
- Git

### Установка и запуск

1. **Клонировать репозиторий:**

```bash
git clone git@github.com:MaxFando/learning_platform.git
cd learning_platform
```

2. **Запустить PostgreSQL через Docker Compose:**

```bash
docker-compose up -d
```

Это создаст контейнер PostgreSQL с параметрами:

- Database: `learning_platform`
- User: `platform_user`
- Password: `secret`
- Port: `5432`

3. **Запустить приложение:**

```bash
./gradlew bootRun
```

Приложение будет доступно по адресу: `http://localhost:8080`

**API доступен по адресу:** `http://localhost:8080/api`

## 📖 Как пользоваться системой

### Вариант 1: Через REST API (рекомендуется)

Система предоставляет полнофункциональный REST API для всех операций.

#### Базовый сценарий использования:

**1. Создайте категорию и пользователей:**

```bash
# Создайте категорию (используйте тестовые данные или напрямую через БД)
# Создайте преподавателя и студента
```

**2. Создайте курс:**

```bash
curl -X POST http://localhost:8080/api/courses \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Introduction to Java",
    "description": "Learn Java programming from scratch",
    "duration": "8 weeks",
    "startDate": "2025-12-01",
    "categoryId": 1,
    "teacherId": 2,
    "tags": ["Java", "Programming", "Beginner"]
  }'
```

**3. Запишите студента на курс:**

```bash
curl -X POST "http://localhost:8080/api/courses/1/enroll?studentId=3"
```

**4. Создайте задание для урока:**

```bash
curl -X POST http://localhost:8080/api/lessons/1/assignments \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Homework 1: Variables and Data Types",
    "description": "Complete exercises 1-10 from the textbook",
    "dueDate": "2025-12-15T23:59:00",
    "maxScore": 100
  }'
```

**5. Студент отправляет решение:**

```bash
curl -X POST http://localhost:8080/api/assignments/1/submit \
  -H "Content-Type: application/json" \
  -d '{
    "studentId": 3,
    "content": "Here is my solution to all exercises..."
  }'
```

**6. Преподаватель оценивает работу:**

```bash
curl -X PUT http://localhost:8080/api/submissions/1/grade \
  -H "Content-Type: application/json" \
  -d '{
    "score": 95,
    "feedback": "Excellent work! Keep it up."
  }'
```

**7. Создайте и пройдите тест:**

```bash
# Создание теста
curl -X POST http://localhost:8080/api/modules/1/quizzes \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Module 1 Quiz",
    "timeLimit": 30,
    "passingScore": 70
  }'

# Прохождение теста
curl -X POST http://localhost:8080/api/quizzes/1/take \
  -H "Content-Type: application/json" \
  -d '{
    "studentId": 3,
    "score": 85
  }'
```

## 🔌 REST API Endpoints

### 📚 Курсы (Courses)

| Метод  | Endpoint                                  | Описание                        |
|--------|-------------------------------------------|---------------------------------|
| POST   | `/api/courses`                            | Создать новый курс              |
| GET    | `/api/courses`                            | Получить все курсы              |
| GET    | `/api/courses/{id}`                       | Получить курс по ID             |
| PUT    | `/api/courses/{id}`                       | Обновить курс                   |
| DELETE | `/api/courses/{id}`                       | Удалить курс                    |
| POST   | `/api/courses/{id}/enroll?studentId={id}` | Записать студента на курс       |
| GET    | `/api/courses/{id}/students`              | Получить список студентов курса |

### 📝 Задания (Assignments)

| Метод | Endpoint                            | Описание                        |
|-------|-------------------------------------|---------------------------------|
| POST  | `/api/lessons/{id}/assignments`     | Создать задание для урока       |
| POST  | `/api/assignments/{id}/submit`      | Отправить решение задания       |
| PUT   | `/api/submissions/{id}/grade`       | Оценить решение                 |
| GET   | `/api/students/{id}/submissions`    | Получить все решения студента   |
| GET   | `/api/assignments/{id}/submissions` | Получить все решения по заданию |

### 🧪 Тесты (Quizzes)

| Метод | Endpoint                          | Описание                     |
|-------|-----------------------------------|------------------------------|
| POST  | `/api/modules/{id}/quizzes`       | Создать тест для модуля      |
| POST  | `/api/quizzes/{id}/questions`     | Добавить вопрос к тесту      |
| POST  | `/api/quizzes/{id}/take`          | Пройти тест                  |
| GET   | `/api/students/{id}/quiz-results` | Получить результаты студента |
| GET   | `/api/quizzes/{id}`               | Получить информацию о тесте  |
| GET   | `/api/quizzes/{id}/questions`     | Получить вопросы теста       |

## 🧪 Как протестировать код

### Запуск всех интеграционных тестов

```bash
./gradlew test
```

## 🎯 Краткий чек-лист использования

1. ✅ Запустить PostgreSQL: `docker-compose up -d`
2. ✅ Запустить приложение: `./gradlew bootRun`
3. ✅ Приложение доступно на `http://localhost:8080`
4. ✅ API доступен на `http://localhost:8080/api`
5. ✅ Создать курс через `POST /api/courses`
6. ✅ Записать студента через `POST /api/courses/{id}/enroll`
7. ✅ Создать задание через `POST /api/lessons/{id}/assignments`
8. ✅ Протестировать через `./gradlew test`
